name: CI Main - Roboshop User
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'db/**'
      - '.github/workflows/ci-db-main.yml'
  workflow_dispatch:

jobs:
  cicd:
    uses: roboshopDevsecops/github-reusable-workflows/.github/workflows/cicd-m.yml@main
